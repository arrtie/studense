<h1>Instructor Profile Details</h1>
<div>
    <h2>Instructor Profile</h2>
    <p>Instructor id: <%= @instructor.id %></p>
    <h2>Profile</h2>
    <ul>
        <% @profile.attributes.each do |key, value| %>
        <li><%= key %> : <%= value%></li>
        <%end%>
    </ul>
</div>